# FixRight: Visual Architecture & Diagrams

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    FIXRIGHT APPLICATION                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   LoginPage  │  │  ProfilePage  │  │  HomePage    │          │
│  │              │  │               │  │              │          │
│  │  - Phone OTP │  │ - Edit Name   │  │ - Show User  │          │
│  │  - Google    │  │ - Edit Addr   │  │ - Show Loc   │          │
│  │  - Location  │  │ - Read-only   │  │ - Location   │          │
│  │    Request   │  │   Phone       │  │   Button     │          │
│  └──────┬───────┘  └──────┬────────┘  └──────┬───────┘          │
│         │                  │                   │                 │
│         └──────────────────┼───────────────────┘                 │
│                            │                                     │
│                    ┌───────▼──────────┐                          │
│                    │  AuthService     │                          │
│                    │  UserService     │                          │
│                    └───────┬──────────┘                          │
│                            │                                     │
│         ┌──────────────────┼──────────────────┐                 │
│         │                  │                  │                 │
│    ┌────▼──────┐   ┌──────▼──────┐   ┌──────▼──────┐           │
│    │ Firebase  │   │  Firestore  │   │ Geolocator │           │
│    │   Auth    │   │             │   │ Geocoding  │           │
│    │           │   │ users/ {     │   │            │           │
│    │ - OTP     │   │   +923001... │   │ - Get Loc  │           │
│    │ - Google  │   │   - phone    │   │ - Geocode  │           │
│    │           │   │   - name     │   │   Address  │           │
│    │           │   │   - address  │   │            │           │
│    │           │   │   - role     │   │            │           │
│    │           │   │   - location │   │            │           │
│    │           │   │ }            │   │            │           │
│    └───────────┘   └──────────────┘   └────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

```
USER STARTS APP
      │
      ▼
┌─────────────────┐
│  Firebase Init  │
└────────┬────────┘
         │
         ▼
┌─────────────────────┐
│  LoginPage (Phone)  │
└────────┬────────────┘
         │
         ▼
┌──────────────────────────────┐
│ verifyPhone() - Send OTP     │
│ (AuthService)                │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ User enters OTP              │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ signInWithOtp()                          │
│ - Authenticate with Firebase             │
│ - Extract: phoneDocId = "+923001234567"  │
└────────┬─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Check if Firestore doc exists   │
│ WHERE id = phoneDocId           │
└────────┬───────────────┬────────┘
         │               │
      NO│               │YES
         ▼               ▼
    ┌────────────┐   ┌──────────┐
    │ Create new │   │ Continue │
    │   user doc │   │  login   │
    │ (role:     │   └──────────┘
    │  buyer)    │        │
    └────┬───────┘        │
         │                │
         └────────┬───────┘
                  │
                  ▼
        ┌─────────────────────────┐
        │ Request Location        │
        │ Permission              │
        └────────┬────────────────┘
                 │
         ┌───────┴────────┐
         │                │
      GRANT             DENY
         │                │
         ▼                ▼
    ┌────────┐         ┌──────┐
    │ Update │         │ Skip │
    │ Loc    │         │ (OK) │
    │ in FS  │         └──────┘
    └────┬───┘            │
         │                │
         └────────┬───────┘
                  │
                  ▼
        ┌──────────────────────┐
        │ Navigate to /home    │
        │ (AppModeSwitcher)    │
        └──────────────────────┘
```

---

## 🗄️ Firestore Database Structure

```
┌─ COLLECTION: users ─────────────────────────────────┐
│                                                      │
│  ┌─ DOCUMENT: +923001234567 (Phone ID) ──────────┐ │
│  │                                                 │ │
│  │  Field: phoneNumber          Value: "+923001234567"
│  │  Field: firebaseUid          Value: "uid123..."    │
│  │  Field: firstName            Value: "Ayaz"         │
│  │  Field: lastName             Value: "Hussain"      │
│  │  Field: address              Value: "Karachi, Pakistan"
│  │  Field: role                 Value: "buyer"        │
│  │                              Options: "buyer" | "seller"
│  │  Field: location (GeoPoint)  {                  │
│  │                                latitude: 24.8607   │
│  │                                longitude: 67.0011  │
│  │                              }                  │
│  │  Field: createdAt (Timestamp) Value: "2024-01-08T10:00:00Z"
│  │  Field: lastLocationUpdate    Value: "2024-01-08T10:05:00Z"
│  │                                                 │
│  └─────────────────────────────────────────────────┘
│                                                      │
│  ┌─ DOCUMENT: +923109876543 (Another User) ──────┐ │
│  │                                                 │ │
│  │  Similar structure...                           │ │
│  │                                                 │ │
│  └─────────────────────────────────────────────────┘
│                                                      │
│  ┌─ DOCUMENT: ... ────────────────────────────────┐ │
│  │  (More users...)                                │ │
│  └─────────────────────────────────────────────────┘
│                                                      │
└──────────────────────────────────────────────────────┘

KEY DESIGN:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Document ID = Phone Number (e.g., +923001234567)
❌ NOT Firebase UID

ADVANTAGES:
• Unique identifier per user
• Easy to query by phone
• User-facing & stable
• Supports account recovery
```

---

## 🔐 Authentication Flow (Detailed)

```
┌──────────────────────────────────────────────────────────────┐
│                  AUTHENTICATION FLOW                          │
└──────────────────────────────────────────────────────────────┘

STEP 1: PHONE ENTRY
┌────────────────────────────┐
│ User selects country:      │
│ Pakistan (+92)             │
│                            │
│ User enters phone:         │
│ 3001234567                 │
│                            │
│ Full number formed:        │
│ +923001234567              │
└────────────────────────────┘
                │
                ▼
STEP 2: OTP VERIFICATION
┌────────────────────────────────────────┐
│ verifyPhoneNumber("+923001234567")     │
│ ↓                                      │
│ Firebase sends OTP to phone            │
│ ↓                                      │
│ User sees: "OTP sent"                  │
│ User enters OTP: 123456                │
└────────────────────────────────────────┘
                │
                ▼
STEP 3: AUTHENTICATION
┌────────────────────────────────────────┐
│ signInWithCredential(credential)       │
│ ↓                                      │
│ Firebase authenticates                 │
│ ↓                                      │
│ Returns: User {                        │
│   uid: "uid123...",                   │
│   phoneNumber: "+923001234567",       │
│   ... other fields                    │
│ }                                     │
└────────────────────────────────────────┘
                │
                ▼
STEP 4: DATABASE CHECK/CREATE
┌────────────────────────────────────────┐
│ Extract: phoneDocId = "+923001234567"  │
│                                        │
│ Query Firestore:                       │
│ users/{phoneDocId} exists?             │
│                                        │
│ ├─ NO: Create document {              │
│ │   phoneNumber: "+923001234567",     │
│ │   firebaseUid: "uid123...",         │
│ │   firstName: "",                    │
│ │   role: "buyer",                    │
│ │   createdAt: now()                  │
│ │ }                                   │
│ │                                     │
│ └─ YES: Continue (user exists)        │
└────────────────────────────────────────┘
                │
                ▼
STEP 5: LOCATION PERMISSION
┌────────────────────────────────────────┐
│ requestPermission():                   │
│ "Location access needed for            │
│  Live Tracking"                        │
│                                        │
│ User response:                         │
│  ├─ Allow: ✓ Update location         │
│  └─ Deny:  ✗ Skip (can enable later)  │
└────────────────────────────────────────┘
                │
                ▼
STEP 6: LOCATION UPDATE (if permitted)
┌────────────────────────────────────────┐
│ getLocation():                         │
│ latitude: 24.8607                      │
│ longitude: 67.0011                     │
│                                        │
│ geocode(lat, lng):                     │
│ "Karachi, Pakistan"                    │
│                                        │
│ Update Firestore:                      │
│ users/{phoneDocId}.update({            │
│   location: GeoPoint(24.8607, 67.0011),│
│   address: "Karachi, Pakistan",        │
│   lastLocationUpdate: now()            │
│ })                                     │
└────────────────────────────────────────┘
                │
                ▼
STEP 7: NAVIGATION
┌────────────────────────────────────────┐
│ pushReplacementNamed("/home")          │
│                                        │
│ AppModeSwitcher → ClientMainScreen    │
│                                        │
│ HomePage displays:                     │
│ "Welcome, Ayaz"                        │
│ "Karachi, Pakistan"                    │
└────────────────────────────────────────┘
```

---

## 📱 UI Navigation Tree

```
LoginPage
├─ Phone Input
│  ├─ Country Picker
│  └─ Phone Number Field
├─ OTP Verification
│  └─ OTP Input Field
└─ Navigation Links
   ├─ Sign Up
   └─ Test Home

         │
         ▼ (after login)

AppModeSwitcher
├─ Client Mode
│  └─ ClientMainScreen
│     ├─ HomePage
│     │  └─ AppBar shows: Name + Location
│     ├─ Messages
│     ├─ Orders
│     ├─ Post Job
│     └─ ProfileScreen ◄─── EDITABLE
│        ├─ "My Profile" Tile
│        │  └─ Edit Dialog
│        │     ├─ First Name (editable)
│        │     ├─ Last Name (editable)
│        │     ├─ Address (editable)
│        │     └─ Phone (read-only)
│        ├─ "My FixRight" Section
│        ├─ Seller Mode Toggle
│        └─ Logout Button
│
└─ Seller Mode
   └─ SellerMainScreen
      ├─ Dashboard
      ├─ Orders
      ├─ Messages
      └─ ProfileScreen (same as client)
```

---

## 🔄 State Management Flow

```
┌─────────────────────────────────┐
│   LoginPageState                │
├─────────────────────────────────┤
│ Variables:                      │
│  - selectedCountry              │
│  - _phoneController             │
│  - _otpController               │
│  - codeSent (bool)              │
│  - verificationId (String)      │
│  - isLoading (bool)             │
└───────────┬─────────────────────┘
            │
            ├──► [AuthService]
            │    - verifyPhone()
            │    - signInWithOtp()
            │    - updateUserLocation()
            │
            └──► [FirebaseAuth]
                 - sendOTP
                 - authenticate

┌─────────────────────────────────┐
│  ProfileScreenState             │
├─────────────────────────────────┤
│ Variables:                      │
│  - phoneDocId (String)          │
│  - firstName (String)           │
│  - lastName (String)            │
│  - address (String)             │
│  - phoneNumber (String)         │
│  - isLoading (bool)             │
└───────────┬─────────────────────┘
            │
            ├──► [AuthService]
            │    - getUserPhoneDocId()
            │    - getUserProfile()
            │
            ├──► [Firestore]
            │    - Get: users/{phoneDocId}
            │    - Update: users/{phoneDocId}
            │
            └──► [EditDialog]
                 - _showEditProfileDialog()
                 - _updateProfile()

┌─────────────────────────────────┐
│  HomePageState                  │
├─────────────────────────────────┤
│ Variables:                      │
│  - userFirstName (String)       │
│  - userLocationAddress (String) │
│  - isLoadingLocation (bool)     │
└───────────┬─────────────────────┘
            │
            ├──► [AuthService]
            │    - getUserPhoneDocId()
            │    - getUserProfile()
            │
            └──► [Firestore]
                 - Get: users/{phoneDocId}
```

---

## 🎨 Profile Edit Dialog Layout

```
┌─────────────────────────────────────┐
│  Edit Profile Dialog                │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │ First Name                  │   │
│  │ [                       ]   │   │
│  │  Input Field                │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ Last Name                   │   │
│  │ [                       ]   │   │
│  │  Input Field                │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ Address                     │   │
│  │ [                           ]   │
│  │ [                           ]   │
│  │ [                           ]   │
│  │  Text Area (3 lines)        │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ Phone Number (Read-Only)    │   │
│  │ [+923001234567          ]   │   │
│  │  Disabled Field             │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌──────────────┬──────────────┐   │
│  │   Cancel     │    Save      │   │
│  │              │              │   │
│  │  [Cancel]    │  [Save]      │   │
│  └──────────────┴──────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

---

## 🔒 Security Flow

```
┌─────────────────────────────────────────────────┐
│           FIREBASE SECURITY RULES               │
├─────────────────────────────────────────────────┤
│                                                 │
│ Rule 1: User can read own document              │
│  ┌─────────────────────────────────┐           │
│  │ allow read: if                  │           │
│  │   request.auth.uid ==           │           │
│  │   resource.data.firebaseUid     │           │
│  └─────────────────────────────────┘           │
│       ↓                                         │
│   ✓ User can view their profile                │
│   ✗ Cannot view other users' profiles          │
│                                                 │
│ Rule 2: User can write own document             │
│  ┌─────────────────────────────────┐           │
│  │ allow write: if                 │           │
│  │   request.auth.uid ==           │           │
│  │   resource.data.firebaseUid     │           │
│  └─────────────────────────────────┘           │
│       ↓                                         │
│   ✓ User can edit their profile                │
│   ✗ Cannot edit other users' profiles          │
│                                                 │
│ Rule 3: New users can be created                │
│  ┌─────────────────────────────────┐           │
│  │ allow create: if                │           │
│  │   request.auth != null          │           │
│  └─────────────────────────────────┘           │
│       ↓                                         │
│   ✓ Authenticated user can create profile      │
│   ✗ Anonymous user cannot                      │
│                                                 │
└─────────────────────────────────────────────────┘

AUTHENTICATION CHAIN:
┌──────────────────┐     ┌──────────────────┐
│  Device User     │────▶│  Firebase Auth   │
│                  │     │  (OTP/Google)    │
└──────────────────┘     └────────┬─────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │  Firebase UID    │
                         │  (Encrypted)     │
                         └────────┬─────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │  Firestore Rules │
                         │  (Verify UID)    │
                         └────────┬─────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │  User Document   │
                         │  (Access Allowed)│
                         └──────────────────┘
```

---

## 📊 Data Sync Diagram

```
┌─────────────────────────────────────────────┐
│         LOCAL STATE ↔ FIRESTORE            │
├─────────────────────────────────────────────┤
│                                             │
│  1. Initial Load (ProfileScreen)            │
│     ┌──────────────┐                       │
│     │ initState()  │                       │
│     └──────┬───────┘                       │
│            ▼                               │
│     _loadUserProfile()                    │
│            │                               │
│            ▼                               │
│     [Firestore Query]                    │
│     SELECT * FROM users                   │
│     WHERE id = phoneDocId                  │
│            │                               │
│            ▼                               │
│     setState() - Update Local State       │
│            │                               │
│            ▼                               │
│     UI Renders with Data                  │
│                                             │
│  2. User Edits (Edit Dialog)                │
│     ┌──────────────────────┐              │
│     │ User clicks "Save"   │              │
│     └──────┬───────────────┘              │
│            ▼                               │
│     _updateProfile()                      │
│            │                               │
│            ▼                               │
│     [Firestore Update]                   │
│     UPDATE users SET                      │
│     firstName = ?, lastName = ?, ...      │
│     WHERE id = phoneDocId                  │
│            │                               │
│            ▼                               │
│     Firestore Server Side                │
│     Validates & Updates                   │
│            │                               │
│            ▼                               │
│     setState() - Update Local State       │
│            │                               │
│            ▼                               │
│     Show Success Message                  │
│                                             │
│  3. Real-time Sync (Optional - Streams)    │
│     ┌──────────────────────┐              │
│     │ StreamBuilder        │              │
│     │ (listen to changes)  │              │
│     └──────┬───────────────┘              │
│            ▼                               │
│     [Firestore Listener]                 │
│     WATCH users/{phoneDocId}              │
│            │                               │
│            ├─ On Change → Update UI       │
│            └─ On Error → Show Error       │
│                                             │
└─────────────────────────────────────────────┘
```

---

**Version:** 1.0.0  
**Last Updated:** January 8, 2026
